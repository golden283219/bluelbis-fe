import React from "react";
// import { useHistory } from "react-router-dom";

const About = () => {
    // var history = useHistory();
    // const { Userdata } = history.location.state;
  return <div>is about page</div>;
};

export default About;
